name: barcodescanner
description: A new flutter plugin project.

flutter:
  plugin:
    androidPackage: ch.swissonid.barcodescanner
    pluginClass: BarcodeScannerPlugin

dependencies:
  flutter:
    sdk: flutter
